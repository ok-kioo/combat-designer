import { Link } from 'react-router-dom';
import { useAuth } from '../../../shared/auth/AuthProvider';
export function LandingPage() {
  const { user } = useAuth();
  return <section className="landing" data-page="landing"><div className="hero"><p className="eyebrow">DO PRIMEIRO GOLPE AO COMBO PERFEITO</p><h1>Faça cada golpe<br /><em>valer a pena.</em></h1><p className="lead">Balanceamento, combos e análise de combate em um só lugar. Transforme os dados do seu jogo em decisões melhores de game design.</p><div className="actions">{user ? <Link className="button primary" to="/workspaces">Ir para Meus Projetos <span aria-hidden="true">→</span></Link> : <><Link className="button primary" to="/register">Criar conta</Link><Link className="button secondary" to="/login">Entrar</Link></>}</div><p className="muted">Seu projeto, suas decisões. Recomendações sem modificar sua engine.</p></div><div className="pillars">{[['01', 'Analise seus golpes', 'Encontre riscos, janelas de cancelamento e oportunidades de balanceamento.'], ['02', 'Construa e simule', 'Explore sequências por personagem e avalie o comportamento do combate.'], ['03', 'Converse com o Director', 'Investigue combos e receba recomendações apoiadas nas análises do seu projeto.']].map(([n, title, description]) => <article className="card" key={n}><span className="eyebrow">{n}</span><h2>{title}</h2><p>{description}</p></article>)}</div><section className="landing-process"><h2>Do seu jogo para uma decisão informada</h2><p>Importe dados → organize personagens → explore combos → analise e refine.</p></section></section>;
}
